name: weeing_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^18.0.1
  workmanager: ^0.9.0+3 # Upgrade to latest version
  permission_handler: ^12.0.1
  firebase_core: ^4.2.1
  firebase_messaging: ^16.0.4
  web_socket_channel: ^2.4.0
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.3.10

flutter_icons:
  android: true
  ios: true
  image_path: "Weeing_logo.png"

flutter_native_splash:
  image: Weeing_splash.png
  color: "#FFFFFF" # 원하는 배경색
  android: true
  ios: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
